PROJECT_NAME="Discountee API"
API_V1_PREFIX="/api/v1"
CORS_ORIGINS="http://localhost:3000,http://localhost:3100"

# Dev default (used if unset): a local SQLite file — zero infra.
DATABASE_URL="sqlite:///./discountee.db"

# Production / Postgres (start ../infra docker-compose first):
# DATABASE_URL="postgresql+psycopg://postgres:postgres@localhost:5432/discountee"
